const heartbeat = require('./heartbeat');
const feedback = require('./feedback');
const generateAuthTokens = require('./generate-auth-tokens');

module.exports = {
  heartbeat,
  feedback,
  generateAuthTokens,
};
